async def get_badge(self): 
        response = request.get(url=f'https://badges.roblox.com/v1/users/{usersId}/badges')
        json = response.json()
        badges = []
        for badges in json["data"]: 
            badges.append(User(badges["name"], badges["id"]))
        return json["badges"]
